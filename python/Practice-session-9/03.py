# 3.	What is the use of default arguments in Python functions?
# Default arguments in Python functions are parameters that are assigned a default value in the
# function definition. If the caller does not provide a value for a parameter with a default argument,
# the function automatically uses the default value.